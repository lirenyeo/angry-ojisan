# Angry Selfie Mode uses Gemini to cut your face out and apply the emotion you pick.
GEMINI_API_KEY=AIza...
GEMINI_IMAGE_MODEL=gemini-3.1-flash-image

# Without a key, Selfie Mode is unavailable (no fallback) — Classic Uncles still plays.

# Optional
PORT=3000
